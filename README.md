# goit-rdb-hw-04

## 📌 Опис

Цей репозиторій містить виконане домашнє завдання №4 з реляційних баз даних.  
Мета роботи — створення власної бази даних, наповнення її тестовими даними, а також написання SQL-запитів з використанням `JOIN`, `GROUP BY`, `HAVING`, `ORDER BY`, `LIMIT` та агрегаційних функцій.

---

## 📂 Структура репозиторію

```
goit-rdb-hw-04/
│
├── task_1/                # Створення структури БД
├── task_2/                # Наповнення таблиць тестовими даними
├── task_3/                # JOIN усіх таблиць з бази mydb
├── task_4/                # Аналітичні запити
│   ├── task_4.1.png
│   ├── task_4.2.x.png
│   ├── task_4.3.x.png
│   ├── task_4.4.png
│   ├── task_4.5.png
│   ├── task_4.6.png
│   └── task_4.7.png
│
├── answer_to_question_4.2.pdf   # Текстова відповідь на пункт 4.2
└── README.md
```

---

## 🧩 Завдання 1 — Створення бази даних

Було створено базу даних **LibraryManagement** з такими таблицями:

- `authors`
- `genres`
- `books`
- `users`
- `borrowed_books`

Усі таблиці мають первинні ключі, а також реалізовані зовнішні ключі (`FOREIGN KEY`) для підтримки зв’язків між ними.

---

## ✍️ Завдання 2 — Заповнення тестовими даними

Кожна таблиця була заповнена мінімум 1–2 тестовими рядками для перевірки працездатності зв’язків.

---

## 🔗 Завдання 3 — JOIN усіх таблиць

Було написано SQL-запит з використанням `INNER JOIN`, який об’єднує такі таблиці:

- `order_details`
- `orders`
- `customers`
- `employees`
- `shippers`
- `products`
- `suppliers`
- `categories`

Зв’язки побудовані на основі спільних ключів.

---

## 📊 Завдання 4 — Аналітичні запити

### 4.1 Підрахунок кількості рядків

Використано `COUNT(*)` для визначення кількості рядків у результаті.

---

### 4.2 Заміна INNER JOIN на LEFT / RIGHT JOIN

У цьому пункті досліджувалась різниця між:

- `INNER JOIN` — повертає лише ті записи, які мають відповідності в обох таблицях.
- `LEFT JOIN` — повертає всі записи з лівої таблиці та відповідні з правої.
- `RIGHT JOIN` — повертає всі записи з правої таблиці та відповідні з лівої.

Було встановлено, що зміна типу JOIN може змінювати кількість рядків у результаті залежно від наявності або відсутності відповідних значень у пов’язаних таблицях.

Детальне пояснення наведене у файлі:
**answer_to_question_4.2.pdf**

---

### 4.3 Фільтрація за employee_id

Було обрано лише ті рядки, де:

```
employee_id > 3 AND employee_id ≤ 10
```

---

### 4.4 Групування

Дані згруповано за назвою категорії (`categories.name`) з підрахунком:

- кількості рядків у групі
- середньої кількості товару (`AVG(order_details.quantity)`)

---

### 4.5 Фільтрація по середньому значенню

Залишено лише ті категорії, де середня кількість товару більша за 21.

---

### 4.6 Сортування

Результат відсортовано за спаданням кількості рядків.

---

### 4.7 OFFSET + LIMIT

Виведено 4 рядки з пропуском першого рядка:

```sql
LIMIT 4 OFFSET 1
```

---

## ✅ Висновок

У процесі виконання роботи було:

- Створено власну БД
- Реалізовано зв’язки між таблицями
- Написано складні JOIN-запити
- Виконано агрегацію, фільтрацію, сортування
- Проаналізовано поведінку різних типів JOIN
- Оформлено результати у вигляді скриншотів та PDF

Робота виконана відповідно до вимог завдання.
